/**
 * Case-study bodies are plain Markdown (headings, lists, links) styled by the
 * `.prose-content` rules in globals.css, so no custom element overrides are needed.
 * Add entries here if a future project uses custom MDX components.
 */
export const mdxComponents = {};
